#include<stdio.h>
int main()
{
  srand(time(NULL));
  int choice, array[100], arrayrand[100], chislo1, chislo2, currentcounter = 0, biggestcounter = 0, currentcounter1 = 0, biggestcounter1 = 0, userchoice, frequentEl1, frequentEl;
  printf("Input 1 for manual or 0 for random\n");
  while (scanf("%d", &userchoice) != 1 || userchoice > 1 || userchoice < 0 || getchar() != '\n')
  {
    printf("Error, try again\n");
    rewind(stdin);
  }
  printf("Imput the number of the elements ");
  while (scanf("%d", &choice) != 1 && choice > 100 || getchar() != '\n')
  {
    for (int i = 0; i < choice; i++)
    {
      printf("Error, try again\n");
      rewind(stdin);
    }
  }
  if (userchoice == 1)
  {
    for (int i = 0; i < choice; i++)
    {
      printf("input %d element in array\n", i + 1);
      while (scanf("%d", &array[i]) != 1)
      {
        printf("Error, try again\n");
        rewind(stdin);
      }
    }
    for (int i = 0; i < choice; i++)
    {
      if (array[i] % 2 == 0)
      {
        for (int j = 0; j < choice; j++)
        {
          if (array[i] == array[j])
          {
            currentcounter++;
          }
          
        }
      }
    }
    for (int i = 0; i < choice; i++)
    {
      if (currentcounter > biggestcounter)
      {
        biggestcounter = currentcounter;
        frequentEl = array[i];
      }
    }
    printf("%d\n", frequentEl);
  }
  if (userchoice == 0)
  {
    for (int i = 0; i < choice; i++)
    {
      arrayrand[i] = rand()%11-10;
      printf("\t%d", arrayrand[i]);
    }
    printf("\n");
    int p;
    for (int i = 0; i < choice; i++)
    {
      for (p = 0; p < choice; p++)
      {
        if (arrayrand[i] % 2 == 0 && arrayrand[i] == arrayrand[p])
        {
          arrayrand[p] == arrayrand[i];
          currentcounter1++;
        }
        
      }
      
    }
    
    for (int i = 0; i < choice; i++)
    {
      if (currentcounter1 > biggestcounter1)
      {
        biggestcounter1 = currentcounter1;
        frequentEl1 = arrayrand[i];

      }
    }
    currentcounter1 = 0;
    printf("%d", frequentEl1);
  }
  return 0;
}
